# MediaMTX config: pull local RTSP and expose HLS for browser playback
logLevel: info

hls:
  enabled: yes
  variant: fmp4
  segmentCount: 3
  segmentDuration: 2s
  allowOrigin: "*"

webrtc:
  enabled: no

paths:
  deepstream:
    source: rtsp://127.0.0.1:8554/deepstream
    sourceOnDemand: yes
